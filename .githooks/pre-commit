#!/bin/sh
set -eu
if ! command -v gitleaks >/dev/null 2>&1; then
    printf '%s\n' 'gitleaks is required for this optional release hook.' >&2
    exit 1
fi
exec gitleaks git --config tools/gitleaks.toml --ignore-gitleaks-allow --pre-commit --staged --redact --no-banner .
